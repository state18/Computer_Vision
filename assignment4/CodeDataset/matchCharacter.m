function [ match_results ] = matchCharacter( input_patches )
% Given a cell array of binary image patches, compare it to the ground truth binary image
% patches of labeled characters and determine if it matches any of them.
% The output is possible matches and their confidence scores computed
% through template matching...


match_results = zeros(length(input_patches),1);



end

